"use strict";

// Compatibilidade com arquivos antigos que ainda importam ../whatsapp/whatsappService.
// A implementação real fica exclusivamente em services/whatsappService.js.
module.exports = require("../services/whatsappService");
